diff --git a/src/mn/mncommon/mnmodeselect.c b/src/mn/mncommon/mnmodeselect.c
index 36314fd..5b3f104 100644
--- a/src/mn/mncommon/mnmodeselect.c
+++ b/src/mn/mncommon/mnmodeselect.c
@@ -1,4 +1,18 @@
 #include <mn/menu.h>
+#ifdef __EMSCRIPTEN__
+extern int port_yougame_menu_context;
+extern int port_yougame_queue_kind;
+extern void port_yougame_menu_font(void);
+extern void port_yougame_menu_text(GObj *, const char *, float, float, float, int);
+extern void port_yougame_online_start(void);
+extern void port_yougame_online_run(void);
+extern void port_yougame_choose_fighter(int);
+#endif
+
+#ifdef __EMSCRIPTEN__
+#define nMNModeSelectOptionEnd 4
+static GObj *sYouGameOnline;
+#endif
 #include <sc/scene.h>
 #include <gm/gmsound.h>
 #include <sys/controller.h>
@@ -754,6 +768,19 @@ void mnModeSelectMakeOptions(void)
     mnModeSelectMakeVSMode();
     mnModeSelectMakeOption();
     mnModeSelectMakeData();
+#ifdef __EMSCRIPTEN__
+    {
+        SObj *sobj;
+        sYouGameOnline = gcMakeGObjSPAfter(0, NULL, 3, GOBJ_PRIORITY_DEFAULT);
+        gcAddGObjDisplay(sYouGameOnline, lbCommonDrawSObjAttr, 1, GOBJ_PRIORITY_DEFAULT, ~0);
+        sobj = lbCommonMakeSObjForGObj(sYouGameOnline, lbRelocGetFileData(Sprite*, sMNModeSelectFiles[1],
+            sMNModeSelectOption == 4 ? llMNMainConsoleIconSprite : llMNMainConsoleIconDarkSprite));
+        sobj->sprite.attr = (sobj->sprite.attr & ~SP_FASTCOPY) | SP_TRANSPARENT;
+        sobj->sprite.red = sobj->sprite.green = sobj->sprite.blue = sMNModeSelectOption == 4 ? 255 : 150;
+        sobj->pos.x = 12; sobj->pos.y = 175;
+        port_yougame_menu_text(sYouGameOnline, "ONLINE", 68, 202, 1.5F, 0xFF0000);
+    }
+#endif
 }
 
 // 0x80132510
@@ -763,6 +790,9 @@ void mnModeSelectEjectOptions(void)
     gcEjectGObj(sMNModeSelectOptionVSModeGObj);
     gcEjectGObj(sMNModeSelectOptionOptionGObj);
     gcEjectGObj(sMNModeSelectOptionDataGObj);
+#ifdef __EMSCRIPTEN__
+    gcEjectGObj(sYouGameOnline);
+#endif
 }
 
 // 0x80132558
@@ -790,6 +820,10 @@ void mnModeSelectInitVars(void)
         sMNModeSelectOption = nMNModeSelectOptionData;
         break;
     }
+#ifdef __EMSCRIPTEN__
+    if (port_yougame_menu_context) sMNModeSelectOption = 4;
+    port_yougame_menu_context = 0;
+#endif
     sMNModeSelectOptionChangeWait = 0;
     
     sMNModeSelectTotalTimeTics = 0;
@@ -838,6 +872,15 @@ void mnModeSelectFuncRun(GObj *gobj)
         {
             switch (sMNModeSelectOption)
             {
+#ifdef __EMSCRIPTEN__
+            case 4:
+                port_yougame_menu_context = 1;
+                func_800269C0_275C0(nSYAudioFGMMenuSelect);
+                gSCManagerSceneData.scene_prev = gSCManagerSceneData.scene_curr;
+                gSCManagerSceneData.scene_curr = nSCKindVSMode;
+                syTaskmanSetLoadScene();
+                return;
+#endif
             case nMNModeSelectOption1PMode:
                 func_800269C0_275C0(nSYAudioFGMMenuSelect);
                 
@@ -984,6 +1027,9 @@ void mnModeSelectFuncStart(void)
     gcMakeDefaultCameraGObj(0, GOBJ_PRIORITY_DEFAULT, 100, 0, GPACK_RGBA8888(0x00, 0x00, 0x00, 0x00));
     
     mnModeSelectInitVars();
+#ifdef __EMSCRIPTEN__
+    port_yougame_menu_font();
+#endif
     mnModeSelectMakeDecalsCamera();
     mnModeSelectMakeLabelsCamera();
     mnModeSelectMakeDecals();
diff --git a/src/mn/mnplayers/mnplayersvs.c b/src/mn/mnplayers/mnplayersvs.c
index 816ac27..ce6ccb3 100644
--- a/src/mn/mnplayers/mnplayersvs.c
+++ b/src/mn/mnplayers/mnplayersvs.c
@@ -2,6 +2,16 @@
 #include <ft/fighter.h>
 #include <if/interface.h>
 #include <mn/menu.h>
+#ifdef __EMSCRIPTEN__
+extern int port_yougame_menu_context;
+extern int port_yougame_queue_kind;
+extern void port_yougame_menu_font(void);
+extern void port_yougame_menu_text(GObj *, const char *, float, float, float, int);
+extern void port_yougame_online_start(void);
+extern void port_yougame_online_run(void);
+extern void port_yougame_choose_fighter(int);
+#endif
+
 #include <sc/scene.h>
 #include <sys/video.h>
 #include <sys/rdp.h>
@@ -2522,6 +2532,10 @@ sb32 mnPlayersVSCheckPuckInRange(GObj *gobj, s32 cursor_player, s32 player)
 // 0x80135C84
 void mnPlayersVSUpdatePlayerKind(s32 player)
 {
+#ifdef __EMSCRIPTEN__
+    if (port_yougame_menu_context) return;
+#endif
+
 #ifdef PORT
 	{
 		/* Classic Co-op context: only two players exist in a classic run —
@@ -4994,6 +5008,11 @@ sb32 mnPlayersVSCheckNoPuckOnPortraitAll(void)
 // 0x8013A5E4
 sb32 mnPlayersVSCheckReady(void)
 {
+#ifdef __EMSCRIPTEN__
+    if (port_yougame_menu_context)
+        return sMNPlayersVSSlots[0].pkind == nFTPlayerKindMan && sMNPlayersVSSlots[0].is_fighter_selected;
+#endif
+
 	sb32 unused;
 	sb32 is_ready = TRUE;
 
@@ -5572,6 +5591,14 @@ void mnPlayersVSFuncRun(GObj *gobj)
 					return;
 				}
 			}
+#endif
+#ifdef __EMSCRIPTEN__
+            if (port_yougame_menu_context) {
+                port_yougame_choose_fighter(sMNPlayersVSSlots[0].fkind);
+                gSCManagerSceneData.scene_curr = nSCKindVSMode;
+                syTaskmanSetLoadScene();
+                return;
+            }
 #endif
 			if (gSCManagerTransferBattleState.is_stage_select != FALSE)
 			{
@@ -5798,6 +5825,14 @@ void mnPlayersVSInitVars(void)
 {
 	s32 i;
 
+#ifdef __EMSCRIPTEN__
+    if (port_yougame_menu_context) {
+        gSCManagerTransferBattleState.is_reset_players = TRUE;
+        gSCManagerTransferBattleState.game_rules = SCBATTLE_GAMERULE_STOCK;
+        gSCManagerTransferBattleState.stocks = 2;
+        gSCManagerTransferBattleState.is_team_battle = FALSE;
+    }
+#endif
 	sMNPlayersVSTotalTimeTics = 0;
 	sMNPlayersVSReturnTic = sMNPlayersVSTotalTimeTics + I_MIN_TO_TICS(5);
 	sMNPlayersVSTimeValue = gSCManagerTransferBattleState.time_limit;
diff --git a/src/mn/mnvsmode/mnvsmode.c b/src/mn/mnvsmode/mnvsmode.c
index e84dca6..8577ca5 100644
--- a/src/mn/mnvsmode/mnvsmode.c
+++ b/src/mn/mnvsmode/mnvsmode.c
@@ -1,4 +1,14 @@
 #include <mn/menu.h>
+#ifdef __EMSCRIPTEN__
+extern int port_yougame_menu_context;
+extern int port_yougame_queue_kind;
+extern void port_yougame_menu_font(void);
+extern void port_yougame_menu_text(GObj *, const char *, float, float, float, int);
+extern void port_yougame_online_start(void);
+extern void port_yougame_online_run(void);
+extern void port_yougame_choose_fighter(int);
+#endif
+
 #include <gm/gmsound.h>
 #include <sc/scene.h>
 #include <sys/video.h>
@@ -1281,6 +1291,10 @@ void mnVSModeSetCostumesAndShades(void)
 // 0x80133B8C
 void mnVSModeMain(GObj *gobj)
 {
+#ifdef __EMSCRIPTEN__
+    if (port_yougame_menu_context) { port_yougame_online_run(); return; }
+#endif
+
     s32 unused;
 
     // 0x80134870
@@ -1645,6 +1659,9 @@ void mnVSModeFuncStart(void)
     mnVSModeMakeButtonViewport();
     mnVSModeMakeButtonValuegSYRdpViewport();
     mnVSModeMakeBackground();
+#ifdef __EMSCRIPTEN__
+    if (port_yougame_menu_context) { port_yougame_online_start(); return; }
+#endif
     mnVSModeMakeMenuName();
     mnVSModeMakeVSStartButton();
     mnVSModeMakeRuleButton();
diff --git a/src/sc/scmanager.c b/src/sc/scmanager.c
index 2b4accc..05dae7c 100644
--- a/src/sc/scmanager.c
+++ b/src/sc/scmanager.c
@@ -1277,6 +1277,13 @@ void scManagerRunLoop(sb32 arg)
 			         (int)gSCManagerTransferBattleState.damage_ratio);
 		}
 	}
+#ifdef __EMSCRIPTEN__
+	if (getenv("SSB64_YOUGAME") != NULL) {
+		/* Fixed duel rules; online never inherits item toggles or CPU seats. */
+		gSCManagerTransferBattleState.item_toggles = 0;
+		gSCManagerTransferBattleState.item_appearance_rate = nSCBattleItemSwitchNone;
+	}
+#endif
 	port_log("SSB64: scManagerRunLoop — controllers=%d scene=%d\n",
 	         (int)gSYControllerConnectedNum, (int)gSCManagerSceneData.scene_curr);
 #endif
